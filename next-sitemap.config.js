/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://sert121.github.io', // Replace with your GitHub Pages URL
    generateRobotsTxt: true, // Optionally generate robots.txt
    outDir: 'out', // Ensure the sitemap is generated in the static output folder
  };
  